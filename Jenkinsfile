stage('Build') {
    shell "echo 'Building..'"
     checkout scm
    //get the source code, compile, run unit tests and buld artifacts
 }
 stage('Test') {
    shell "echo 'Testing..'"
 }
 stage('Deploy') {
    shell  "echo 'Deploying....'"
 }


