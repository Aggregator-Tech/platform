ext {
    servicePort='9501'
}

apply from: "$rootProject.projectDir/gradle/script/web.gradle"
apply from: "$rootProject.projectDir/gradle/script/docker.gradle"

dependencies {
    implementation(
            project(':web')
    )

}

task publishD(overwrite: true) { 
        print "hello from publishD"
    
         tasks.publishToDockerHub.execute()
     
}
